generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model drawers {
  id                Int                 @id @default(autoincrement())
  name              String              @db.VarChar
  user_id           Int
  users             users               @relation(fields: [user_id], references: [id], onDelete: Cascade)
  drawers_and_words drawers_and_words[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model drawers_and_words {
  drawer_id Int
  word_id   Int
  drawers   drawers @relation(fields: [drawer_id], references: [id], onDelete: Cascade)
  words     words   @relation(fields: [word_id], references: [id], onDelete: Cascade)

  @@id([drawer_id, word_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model school_partners {
  school_email       String    @id @db.VarChar
  active_partnership String    @db.VarChar
  school_name        String?   @db.VarChar
  schools            schools[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schools {
  id              Int             @id @default(autoincrement())
  email           String          @db.VarChar
  password        String?         @db.VarChar
  name            String?         @db.VarChar
  school_partners school_partners @relation(fields: [email], references: [school_email], onDelete: Cascade)
  subscriptions   subscriptions[]
  users           users[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model subscriptions {
  email               String    @id @db.VarChar
  key_expiration_date DateTime? @db.Date
  school_id           Int?
  languages           String?
  schools             schools?  @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model users {
  id                            Int       @id @unique(map: "id") @default(autoincrement())
  first_name                    String
  last_name                     String
  email                         String    @unique(map: "unique_email")
  password                      String
  refresh_token                 String?
  refresh_token_expiration_date DateTime?
  email_verified                Boolean?  @default(false)
  school_id                     Int?
  languages                     String?   @db.VarChar
  drawers                       drawers[]
  schools                       schools?  @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  words                         words[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model words {
  id                Int                 @id @unique @default(autoincrement())
  word              String
  status            Boolean
  user_notes        String
  generated_notes   String
  audio             String
  repetitions       Int
  days              Int
  review_date       DateTime
  ease_factor       Float
  user_id           Int
  language          String
  drawers_and_words drawers_and_words[]
  users             users               @relation(fields: [user_id], references: [id], onDelete: Cascade)
}
